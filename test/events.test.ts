describe('Bind events to interests point', () => {
  beforeAll(async () => {
    // init db here
  });

  afterAll(async () => {
    // clean db here
  });

  it('Should  belong events to interests point', async () => {
    // todo
  });
});
